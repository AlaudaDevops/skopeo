# Temporary ignore for unresolvable docker module CVEs.
# Root cause: go.podman.io/image/v5 currently depends on github.com/docker/docker@v28.x.
# Trivy fixed version points to v29.3.1, but that revision has moved to module path github.com/moby/moby/v2,
# which is not yet consumable through current dependency chain without a larger upstream migration.
# Track upstream migration in go.podman.io/image and remove these ignores once module path is updated.
CVE-2026-34040
CVE-2026-33997
